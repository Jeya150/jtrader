import {createFileRoute} from '@tanstack/react-router';import {db,active} from '../../lib/course.server';import {bindings} from '../../lib/bindings.server';
export const Route=createFileRoute('/api/video')({server:{handlers:{GET:async({request})=>{await active(request);const id=new URL(request.url).searchParams.get('lesson');if(!id)return new Response('Missing lesson',{status:400});const l=await db().prepare('SELECT video_key FROM lessons WHERE id=?').bind(id).first<any>();if(!l?.video_key)return new Response('Video not found',{status:404});const o=await bindings().STORAGE?.get(l.video_key);if(!o)return new Response('Video not found',{status:404});return new Response(o.body as any,{headers:{'Content-Type':o.httpMetadata?.contentType||'video/mp4','Cache-Control':'private,no-store'}})}}}})
