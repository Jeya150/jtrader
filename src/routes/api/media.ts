import {createFileRoute} from '@tanstack/react-router';import {bindings} from '../../lib/bindings.server';
export const Route=createFileRoute('/api/media')({server:{handlers:{GET:async({request})=>{const key=new URL(request.url).searchParams.get('key')||'';if(!key.startsWith('course/thumbnail/'))return new Response('Not found',{status:404});const o=await bindings().STORAGE?.get(key);if(!o)return new Response('Not found',{status:404});return new Response(o.body as any,{headers:{'Content-Type':o.httpMetadata?.contentType||'image/jpeg','Cache-Control':'public,max-age=31536000,immutable'}})}}}})
